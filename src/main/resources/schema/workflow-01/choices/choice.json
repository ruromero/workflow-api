{
  "type": "object",
  "javaType": "org.serverless.workflow.api.choices.Choice",
  "description": "Choice",
  "properties": {
    "condition": {
      "type": "object",
      "existingJavaType": "org.serverless.workflow.api.interfaces.Condition"
    },
    "nextState": {
      "type": "string",
      "description": "Specifies the name of the next state to transition to if there is a value match",
      "minLength": 1
    },
    "metadata": {
      "$ref": "../metadata/metadata.json"
    }
  },
  "required": [
    "nextState"
  ]
}