{
  "type": "object",
  "javaType": "org.serverless.workflow.api.branches.Branch",
  "description": "Branch Definition",
  "properties": {
    "name": {
      "type": "string",
      "description": "Branch name"
    },
    "startAt": {
      "type": "string",
      "description": "Name of the state to start from"
    },
    "states": {
      "type": "array",
      "description": "State Definitions",
      "items": {
        "type": "object",
        "existingJavaType": "org.serverless.workflow.api.interfaces.State"
      }
    },
    "waitForCompletion": {
      "type": "boolean",
      "default": false,
      "description": "Flow must wait for this branch to finish before continuing"
    },
    "metadata": {
      "$ref": "../metadata/metadata.json"
    }
  },
  "required": [
    "name",
    "startAt",
    "states",
    "waitForCompletion"
  ]
}