{
  "type": "object",
  "javaType": "org.serverless.workflow.api.timeout.Timeout",
  "description": "Timeout definition",
  "properties": {
    "period": {
      "type": "string",
      "description": "Specifies the time period waiting for the events in the EVENTS-EXPRESSION (ISO 8601 format)"
    },
    "then": {
      "type": "string",
      "description": "Name of the state to transition to if the timeout period is reached"
    },
    "metadata": {
      "$ref": "../metadata/metadata.json"
    }
  },
  "required": [
    "period",
    "then"
  ]
}